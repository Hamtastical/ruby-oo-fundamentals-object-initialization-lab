class Dog
    def initialize(name,breed="Mutt") #Able to define breed within initialize to be default if no breed is given
    @name = name #set name to instance variable
    @breed = breed #set breed to instance variable
    end

end
